---
trigger: model_decision
description: When a new feature, DataType, or method is added.
---

Always write corresponding unit tests using Pest. Ensure edge cases (like missing parameters or invalid formats) are covered. Do not mock internal package classes; test the actual output generation where possible.
