---
trigger: model_decision
description: When generating or modifying PHP classes.
---

Always include declare(strict_types=1); at the top of every PHP file. Enforce native PHP 8.2+ return types, parameter types, and property types. Avoid using mixed unless absolutely necessary. Use native Enums for predefined values.
